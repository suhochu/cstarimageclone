import 'package:flutter/material.dart';

class PersonalColorConsultantPage extends StatelessWidget {
  static const String routeName = 'certification_personal_color_consultant';
  const PersonalColorConsultantPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('자격증과정 (퍼스널컬러 컨설턴트 자격증)'),
    );
  }
}