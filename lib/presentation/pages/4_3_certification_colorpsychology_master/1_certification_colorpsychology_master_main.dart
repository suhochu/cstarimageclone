import 'package:flutter/material.dart';

class ColorPsychologyMasterPage extends StatelessWidget {
  static const String routeName = 'certification_color_psychology_master';
  const ColorPsychologyMasterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('자격증과정 (색체심리 마스터 자격증)'),
    );
  }
}